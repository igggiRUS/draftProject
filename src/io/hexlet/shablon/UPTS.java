package io.hexlet.shablon;

public class UPTS {
    public static void main(String[] args) {
//       ввод в метод
        String upString = uptocasepattern("AfrAck!");
//        вывод из метода
        System.out.println(upString);
    }
    public static String uptocasepattern(String str) {
//        var result = "";
//        for (var i = 0; i < str.length(); i += 2) {
//            var nextSymbol = ((str.charAt(i)).equals(str.toUpperCase()) ? "" : str.substring(i + 1, i + 2);
//            result = result + nextSymbol + str.charAt(i);
//        }

        System.out.println();
        return str;
    }
}

